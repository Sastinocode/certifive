# xlsx (SheetJS) tiene CVEs conocidos (Prototype Pollution, ReDoS) pero solo
# se usan sus funciones de ESCRITURA, nunca de parseo/lectura de archivos.
# Los CVEs solo se explotan al parsear Excel malicioso — no aplica aquí.
# Revisión pendiente: migrar a write-excel-file cuando haya tiempo.
audit-level=critical
